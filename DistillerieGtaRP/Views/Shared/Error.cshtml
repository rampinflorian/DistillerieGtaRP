@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Erreur.</h1>
<h2 class="text-danger">Y'a une fuite dans la distillerie !!!</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>BIP BOOP BIP BOOP </h3>
<p>
    Communiquez le code erreur ci-dessus au développeur de l'application (et une petite bière pour que le boulot soit fait rapidement)
</p>